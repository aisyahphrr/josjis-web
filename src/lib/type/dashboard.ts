export type DashboardRole = "user" | "umkm" | "admin" | "driver";

export type OrderStatus =
  | "delivered"
  | "shipping"
  | "processing"
  | "cancelled"
  | "pending"
  | "shipped";

export type ComplaintStatus = "open" | "in-progress" | "resolved";
